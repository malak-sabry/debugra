[tool.uv.workspace]
members = [
    "packages/schemas",
    "apps/orchestrator",
    "apps/agent-runner",
    "suts/lms/backend",
    "suts/shop/backend",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "ruff>=0.8.0",
]
